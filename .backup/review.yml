name: Review Deployment

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize

jobs:
  review:
    name: Review Deployment
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
